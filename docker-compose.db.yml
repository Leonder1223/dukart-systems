services:
  postgres:
    image: postgres:16
    container_name: dukart-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: dukart_user
      POSTGRES_PASSWORD: MwPiCoMpiS18!
      POSTGRES_DB: dukart_portal
    ports:
      - "5432:5432"
    volumes:
      - dukart_postgres_data:/var/lib/postgresql/data

volumes:
  dukart_postgres_data:
